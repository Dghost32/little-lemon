import Screen from "@/components/Screen";
import ProfileForm from "./profileForm";

const ProfileScreen = () => {
  return (
    <Screen>
      <ProfileForm />
    </Screen>
  );
};

export default ProfileScreen;
